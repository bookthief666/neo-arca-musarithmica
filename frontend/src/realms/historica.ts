import type { ArcaRealmDefinition } from './types'

export const HISTORICA_REALM = Object.freeze<ArcaRealmDefinition>({
  id: 'historica',
  label: 'Arca Historica',
  ceremonialLabel: 'Reliquary Cabinet',
  availability: 'available',
  cssScope: 'historica',
  guidanceVoice: 'scholarly',
})
