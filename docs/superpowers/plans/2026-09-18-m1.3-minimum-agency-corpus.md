# M1.3 — Minimum Agency Corpus Implementation Plan

**Milestone:** M1.3 — Minimum Agency Corpus

**Design authority:** `docs/superpowers/specs/2026-09-18-m1.3-minimum-agency-corpus-design.md`

**Design branch authority:** `feature/m1.3-minimum-agency-corpus-design@7b372ce1791419a6cd12321fa4ce833b9228d9a2`

**Implementation starting authority:** `feature/m1.2.1r-historical-computation-rescue@679df636502c8af8fd47a47d19e1edddfecf7255`

**Do not merge/rebase/reset the M1.2.1R lineage.**

**M0.9 baseline remains immutable:** `baseline/m0.9-arca-historica-first-fragment@06c76a2cb857e49ec64d7b8049bb4ce18a9ca887`

## Execution rule

M1.3 is a bounded historical-agency milestone.

Do not start frontend corpus expansion until the three new target Vperms are independently verified from primary witnesses.

If any target record fails verification, stop and revise the corpus boundary. Do not substitute a secondary transcription.

---

## Task 1 — Verify and promote the three new Vperm records

**Target records:**

- S1 / P4 / source column I / Vperm02
- S1 / P4 / source column II / Vperm01
- S1 / P4 / source column II / Vperm02

**Existing record preserved:**

- S1 / P4 / source column I / Vperm01

**Files:**

- Create: `historical_data/syntagma1_pinax04/vperm02_printed_p83_verified.json`
- Create: `historical_data/syntagma1_pinax04/stropha2_vperm01_printed_p83_verified.json`
- Create: `historical_data/syntagma1_pinax04/stropha2_vperm02_printed_p83_verified.json`
- Modify only if required: `historical_data/source_witnesses.json`
- Create/modify focused transcription tests under `tests/`

### Step 1 — establish blind Pass A worksheets

Use a public-domain 1650 primary image before consulting modern numeric transcriptions.

Preferred first witness:

`ERARA_ETH_RAR610_T2_1650`

Printed p.83 = e-rara scan [92].

Record each of the 24 cells per target Vperm independently:

`CANTUS N01..N06`
`ALTUS N01..N06`
`TENOR N01..N06`
`BASSUS N01..N06`

Do not copy values from Cashner/Chierotti into the worksheet.

### Step 2 — independent Pass B

Use a witness with a different `independence_key`, preferably one of:

- `BSB_MUNICH_2_MUS_TH_264_2_1650`
- `IA_EPFL_CHEPFL_LIPR_AXC19_02_1650`
- `BNF_GALLICA_RES_F_142_T2_1650`

Every source cell must agree.

If a cell is ambiguous in either witness:

- preserve the ambiguity;
- mark the record noncanonical;
- stop promotion of that Vperm.

### Step 3 — secondary cross-check only after primary agreement

After Pass A/B are frozen, compare against:

- Andrew Cashner’s Pinax-IV implementation/documentation;
- Chierotti’s study if useful.

A secondary disagreement becomes provenance/discrepancy history; it never overrides primary agreement.

### Step 4 — write canonical records

Each record must satisfy the same authority shape as the existing canonical Vperm01:

- `canonical: true`
- `verification_status: verified`
- exact source-column and Vperm scope
- `voice_order_top_to_bottom`
- exact rows
- verification witness IDs/keys
- complete 24-cell provenance
- `active_value_origin: source_reading`
- no editorial correction

### Step 5 — add RED/GREEN record tests

Tests must reject:

- one-witness records;
- missing cells;
- duplicated witness independence;
- editorial corrections;
- row/provenance disagreement;
- non-1..8 degree values;
- wrong source-column/Vperm scope.

Run:

`python -m pytest tests/test_m13_minimum_agency_data.py -q`

### Step 6 — commit Task 1 only after all three records are H0-verified

Suggested commit:

`data(m1.3): verify minimum Pinax-IV agency corpus`

**STOP if fewer than all three target Vperms verify.**

---

## Task 2 — Add the versioned agency kernel without changing M0.9

**Files:**

- Create: `scripts/arca_historica_agency_kernel.py`
- Create: `tests/test_arca_historica_agency_kernel.py`
- Do not modify: `scripts/arca_historica_kernel.py`

### Step 1 — write failing kernel tests

Define exactly four legal arrangements:

- Column I Vperm01 + Column II Vperm01
- Column I Vperm01 + Column II Vperm02
- Column I Vperm02 + Column II Vperm01
- Column I Vperm02 + Column II Vperm02

For each arrangement assert:

- two ordered source segments;
- six events per segment;
- twelve events total;
- eight relative minim units per segment;
- sixteen total;
- fixed Rperm03;
- fixed printed-p.51 Tone II;
- exact event/source provenance;
- deterministic output.

Assert changing only Column I selection changes only segment I.

Assert changing only Column II selection changes only segment II.

### Step 2 — fail closed

Add tests rejecting:

- source column III/IV;
- Vperm03+;
- missing source column;
- duplicate source column;
- reversed source order if request order is declared canonical;
- client-supplied pitch/rhythm/tone arrays;
- unverified Vperm record;
- mismatched witnesses;
- unavailable Rperm/tone.

### Step 3 — implement new kernel

Create a generalized validator owned by the new kernel.

Do not import secondary transcriptions.

Do not mutate M0.9.

Output provisional format:

`neo-arca-historica-agency/v1`

with:

- `segments[0]` = source column I
- `segments[1]` = source column II
- flattened twelve-event index if useful for presentation
- source-column/Vperm IDs on every segment
- cell provenance on every event

### Step 4 — verify M0.9 unchanged

Run:

`python -m pytest tests/test_arca_historica_kernel.py tests/test_arca_historica_agency_kernel.py -q`

Also compare:

`python scripts/arca_historica_kernel.py --json`

against the accepted M0.9 fixture/snapshot.

### Step 5 — commit

`feat(m1.3): add minimum-agency historical kernel`

---

## Task 3 — Introduce bridge v3 manifest/request/execution

**Files:**

- Modify: `scripts/arca_mechanica_bridge.py`
- Modify: `tests/test_arca_mechanica_bridge.py`
- Add focused v3 bridge tests if clearer.

### Step 1 — define RED contract tests

Manifest v3 must expose exactly:

- source column I;
- source column II;
- two verified Vperm IDs under each;
- fixed Rperm03 policy;
- fixed Tone-II policy;
- manifest/content digest;
- edition/schema identity.

Request v3 contains only:

- format;
- manifest ID;
- content digest;
- carrier I instance/source/Vperm;
- carrier II instance/source/Vperm;
- fixed slot/order identity.

Reject:

- raw pitch arrays;
- raw rhythm arrays;
- tone choice;
- unknown fields;
- stale v2 request shape;
- Vperm ID not listed for the carrier’s source column;
- swapped/mismatched carrier source identity.

### Step 2 — implement server resolution

The bridge resolves record paths and calls the agency kernel.

The client never names filesystem paths.

### Step 3 — preserve v2

Existing M1.2.1R v2 manifest/execution path must remain callable for regression and archaeology.

Do not silently reinterpret v2 as v3.

### Step 4 — run bridge/kernel gates

`python -m pytest tests/test_arca_mechanica_bridge.py tests/test_arca_historica_kernel.py tests/test_arca_historica_agency_kernel.py -q`

### Step 5 — commit

`feat(m1.3): expose verified agency corpus through bridge v3`

---

## Task 4 — Atomically migrate TypeScript contracts and reducer to two carriers

**Files likely touched:**

- `frontend/src/instrument/types.ts`
- `frontend/src/instrument/contracts.ts`
- `frontend/src/instrument/model.ts`
- `frontend/src/instrument/reading.ts`
- associated tests
- direct consumer compile fixes required by the migration

### Step 1 — RED runtime-decoder tests

Require strict v3 decoding:

- exactly two source-column authorities;
- two choices each;
- stable source/Vperm IDs;
- fixed rhythm/tone policy;
- no unknown musical fields.

### Step 2 — RED reducer tests

State invariants:

- maximum two carrier instances;
- exactly one carrier per source column;
- persistent instance identity;
- fixed slot I/II;
- two legal Vperm selections per carrier;
- no hidden/dead row index;
- row selection changes request identity;
- carrier location/selection survives error/retry/revelation/return.

### Step 3 — canonical actions

Prefer source-semantic actions, e.g.:

- `RETRIEVE_SOURCE_CARRIER`
- `PLACE_SOURCE_CARRIER`
- `SET_CARRIER_VPERM`

Do not reintroduce generic `MOVE_ROD` or old “band” vocabulary.

### Step 4 — request builder

The request builder emits only authority IDs and selections.

No degree/rhythm arrays leave the client.

### Step 5 — migrate all direct consumers in the same task

Do not leave compatibility state or an App-level adapter that preserves the one-carrier schema.

Task 4 must end compile-clean.

Run:

`cd frontend`
`npm test -- --run src/instrument`
`npm run typecheck`

### Step 6 — commit

`feat(m1.3): migrate canonical state to two-source agency`

---

## Task 5 — Build two truthful source-column carriers

**Files:**

- spatial carrier/carriage components
- dimensions/stations
- deterministic carrier textures
- focused geometry/model tests

### Step 1 — RED geometry tests

Require:

- exactly two operative workbench slots;
- source column I and II have distinct physical identity;
- each rod face exposes exactly two verified Vperm bands;
- no third selectable band;
- no duplicate musical state in presentation.

### Step 2 — Cell IV presentation

Cell IV contains the two distinct source-column carriers.

They must be visually distinguishable before interaction.

Use concise source labels rather than relying on Instrument Controls.

### Step 3 — workbench slots

Seat them side-by-side:

`COLUMN I | COLUMN II`

Slots are fixed in historical sequence for this milestone.

### Step 4 — textures/materials

Each carrier must visibly print its two H0 Vperm blocks and H0/H1 authority labels at readable Fold scale.

Generated resources must be deterministically disposed.

### Step 5 — run gates and commit

`npm test -- --run <carrier/station/renderer suites>`
`npm run typecheck`

Commit:

`feat(m1.3): build the two-column historical workbench`

---

## Task 6 — Make vertical rod selection physically real

### Step 1 — RED interaction tests

For both carriers:

- row 1 and row 2 directly reachable;
- zero/nonzero carriage travel;
- translated-axis projection invariant;
- drag outside target releases;
- pointer cancel releases;
- unmount releases;
- orbit suppressed during carrier drag;
- orbit resumes immediately afterward.

### Step 2 — generous Fold touch surface

The visible rod body is the drag target.

Do not require the finial.

Use invisible hit padding only if it follows the visible object and does not create hidden unrelated hotspots.

### Step 3 — canonical selection

Physical displacement dispatches `SET_CARRIER_VPERM` using manifest-listed source IDs.

No DOM bridge.

### Step 4 — mobile-touch CI

Add Fold-like Chromium:

- `884×1104`
- `isMobile: true`
- `hasTouch: true`
- native touchStart/move/end
- independently drag carrier I and carrier II
- verify all four agency combinations can be reached.

Bound the browser script with explicit timeouts so CI cannot hang indefinitely.

### Step 5 — commit

`feat(m1.3): make historical row selection directly manipulable`

---

## Task 7 — Add true two-column transverse reading

### Step 1 — RED reading-model tests

The reading model must show:

- selected source/Vperm for Column I;
- selected source/Vperm for Column II;
- shared fixed Rperm03;
- fixed Tone II;
- six resulting events for segment I;
- six resulting events for segment II.

Changing one rod changes only the corresponding segment display.

### Step 2 — spatial transverse rule

Render a reading rule spanning both rods.

It must make the selected row on each vertically shifted rod visually legible.

Only now may the product use “transverse reading” language.

### Step 3 — causality

Before LECTIO the operator can inspect:

`rod source row → degree → fixed Tone-II pitch class → Rperm03 duration`

for both segments.

LECTIO validates/consolidates that visible arrangement.

### Step 4 — revelation

The revelation folio identifies the two source segments and preserves their boundary/provenance.

No octave/MIDI/BPM implications.

### Step 5 — commit

`feat(m1.3): expose true transverse two-column reading`

---

## Task 8 — Complete semantic/accessibility parity for the 2×2 matrix

### Step 1 — RED accessibility tests

Instrument Controls must expose:

- both carriers;
- location for each;
- source-column label;
- two Vperm choices per carrier;
- exact selected degree rows;
- fixed Rperm03;
- fixed Tone II;
- segment I/II result;
- LECTIO/retry/return;
- provenance/non-claims.

### Step 2 — same reducer only

No accessibility-only selection state.

### Step 3 — grouped announcements

On rod change announce only the changed source column plus its new selected Vperm and resulting segment summary.

Avoid reading the entire apparatus on each touch.

### Step 4 — focus continuity

Preserve focus through execution failure/retry/revelation/return.

### Step 5 — commit

`feat(m1.3): expose minimum agency semantically`

---

## Task 9 — Provenance, continuity, and regression hardening

### Historical authority

Show:

- each Vperm record’s primary witnesses;
- source-column identity;
- fixed shared Rperm03;
- fixed p.51 Tone II;
- H1 physical carrier/slot/transverse-rule reconstruction;
- modern schema identity.

### Continuity

Assert both carrier IDs and both Vperm selections survive:

- execute;
- bridge rejection;
- parity failure;
- retry;
- successful revelation;
- return.

### M0.9 regression

M0.9 kernel and v2 bridge path must remain unchanged in behavior.

### Performance

Record exact `SpatialArca` raw/gzip/Brotli delta from M1.2.1R final production-code HEAD `63e8472084fdfa544dfb1f15c2f297a2f67a2a4c`.

Resolve or explicitly account for the known Three.js test warning.

### Commit

`fix(m1.3): preserve two-column authority and continuity`

---

## Task 10 — Full automated and owner-release acceptance

### Complete gates

Python:

`python -m pytest -q`

Frontend:

`npm test -- --run`
`npm run typecheck`
`npm run lint`
`npm run build`

Authority scans:

- M0.9 kernel unchanged;
- pre-M1.3 canonical historical records unchanged;
- no secondary-only data promoted;
- no dead bands;
- no `document.body.dataset.arcaGrab`;
- no client-side pitch/rhythm/tone authority.

### Browser QA

Required:

- `360×800`
- `768×1024`
- `884×1104`
- `1104×884`
- `1440×900`

Capture:

- Cell IV with both carriers;
- one carrier held;
- both seated;
- all four Vperm combinations;
- transverse reading;
- bridge rejection/retry;
- revelation;
- legacy renderer compatibility where applicable.

### Mobile-touch automation

Use bounded native-touch emulation for:

- carrier I row 1→2;
- carrier II row 1→2;
- reverse direction;
- off-target release;
- pointer cancel;
- orbit recovery.

### Physical owner smoke

The owner’s real Fold/TalkBack pass may remain deferred until release/freeze under the explicit M1.2.1R continuation decision.

Never label it passed unless the owner actually performs it.

### Evidence document

Create:

`docs/M1_3_MINIMUM_AGENCY_CORPUS.md`

Record:

- base/final SHAs;
- new H0 records and witness evidence;
- all command outputs/counts;
- browser/mobile-touch evidence;
- bundle comparison;
- unresolved source ambiguities;
- real-device verdict status.

### STOP

After M1.3 evidence is committed:

- do not add Column III/IV;
- do not add more Vperms;
- do not add rhythm/tone choices;
- do not begin material-production polish;
- do not open the Ghost/Neo bridge;
- do not merge to a baseline without a new owner decision.
